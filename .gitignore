# Secrets — never commit your API key
.env
.env.*
!.env.example

# Python / uv
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Generated run artifacts (regenerated by `uv run agent.py` / `uv run runner.py`)
trajectory.jsonl
runs/

# Session-log media — text logs are committed, audio never enters git
docs/session-logs/*.mp3
docs/session-logs/*.wav
docs/session-logs/*.m4a
